const log = require('../log/winston').logger('DB Init');

try {
	// log.info('(dbHelper):  Will init DB here!');
	// NGTSReqAck.sync({ alter: true });
	// ...
} catch (error) {
	console.error(error)
}
